# macOS
.DS_Store
.AppleDouble
.LSOverride

# Claude（追蹤 commands/ 與 scripts/，排除其餘）
.claude/worktrees/
.claude/settings.local.json

# Obsidian
.obsidian/

# Google Docs shortcut files
*.gdoc
*.gsheet
*.gslides

# TeacherOS 個人環境設定（每位使用者各自設定，不上傳）
setup/environment.env

# Google Calendar API 憑證（含個人授權，不上傳）
setup/credentials.json
setup/token.json

# 學生個人資料（含真名，不上傳）
projects/*/roster.yaml

# 期末學生評量總檔（含還原的真實姓名，不上傳）
projects/*/english/content/student-assessments*.md
