# TeacherOS — 個人環境設定範本
# ─────────────────────────────────────────────────────────────
# 使用方式：
#   1. 複製本檔案，命名為 environment.env（同一資料夾內）
#   2. 依照你的個人資訊填入以下欄位
#   3. environment.env 已列入 .gitignore，不會上傳至 GitHub
#
# 注意：等號前後不加空格，字串不需引號

# ── 使用者基本資訊 ────────────────────────────────────────────

USER_NAME=你的姓名
USER_EMAIL=你的Email

# ── Google Drive 設定 ─────────────────────────────────────────
#
# 前置條件：
#   1. 安裝 Google Drive for Desktop（https://www.google.com/drive/download/）
#   2. 登入你的 Google 帳號
#   3. 確認「串流模式」或「鏡像模式」已開啟（兩者皆可）
#
# GOOGLE_DRIVE_EMAIL：登入 Google Drive for Desktop 的帳號
# GOOGLE_DRIVE_FOLDER：TeacherOS 在你 Google Drive 內的資料夾名稱
#
# 設定完成後，本機路徑會自動組合為：
#   ~/Library/CloudStorage/GoogleDrive-{GOOGLE_DRIVE_EMAIL}/
#     我的雲端硬碟/{GOOGLE_DRIVE_FOLDER}/projects/

GOOGLE_DRIVE_EMAIL=你的帳號@gmail.com
GOOGLE_DRIVE_FOLDER=00-01-TeacherOS-專案三層記憶

# ── GitHub 設定 ───────────────────────────────────────────────
#
# 前置條件：
#   1. 申請 GitHub 帳號（https://github.com）
#   2. 取得 Repo 存取權限（向系統管理員申請）
#   3. 在本機執行 git clone，將 Repo 下載到電腦

GITHUB_USERNAME=你的GitHub帳號

# ── Pandoc 路徑（通常不需修改）──────────────────────────────
#
# 安裝方式（macOS）：
#   brew install pandoc
#
# 安裝後確認：pandoc --version

PANDOC_PATH=/opt/homebrew/bin/pandoc
