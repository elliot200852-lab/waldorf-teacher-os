# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 【TeacherOS 專案記憶 — Layer 2】
# 名稱：四年級 C 班（台語）
# 路徑：projects/class-4c/project.yaml
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
class: 4C
grade: 四年級
teacher_role: 科任教師
di_framework_ref: projects/_di-framework/project.yaml

# ── 班級檔案索引（AI 讀這裡就知道要載入什麼）────────────────
class_files:
  roster: roster.yaml
  students: students.yaml
  session_state: taiwanese/session.yaml

subjects:
  taiwanese:
    type: 台語專科
    weekly_sessions: 待定
    lesson: taiwanese/lesson.yaml
    di_profile: taiwanese/di-profile.yaml
    assessment: taiwanese/assessment.yaml
    content_output: taiwanese/content/

current_semester: 2025-2026
current_focus: 尚未開始

next_steps:
  - 填入學生 DI 樣態（students.yaml）
  - 開始台語科學季大綱設計

status: active
